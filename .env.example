TZ=UTC
PUID=1000
PGID=1000
MY_DOMAIN=example.com
# The server's Tailscale IP (tailscale ip -4). Caddy's HTTPS port binds to
# this address only, so private hostnames stay unreachable from LAN/WAN.
TAILSCALE_IP=100.64.0.1
